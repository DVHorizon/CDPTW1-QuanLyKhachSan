{
  "name": "hotel-management-backend",
  "version": "1.0.0",
  "description": "Backend API cho Đồ án Quản lý Khách sạn",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "migrate": "echo 'Chạy migration...' && exit 0",
    "seed": "echo 'Chạy seeder...' && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mysql2": "^3.9.0",
    "sequelize": "^6.37.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
